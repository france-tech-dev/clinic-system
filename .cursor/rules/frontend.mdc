---
globs: *.tsx
alwaysApply: false
---

# Front-end

## Componentes

- **Server** por padrão (async, fetch/Prisma no servidor).
- **`'use client'`** só para interatividade, hooks, formulários com estado.

## Dados e mutações

- Leitura: Server Components + Prisma ou `fetch` com `revalidate`/`tags`.
- Mutação: Server Actions em `src/features/[feature]/actions.ts` (ver `nextjs-server-actions.mdc`).
- Cliente: `useActionState` em forms; `useTransition` em ações por clique.
- **Formulários com validação:** `useForm` + `zodResolver` + `Form`/`FormMessage`/`FormLabel` com `*` nos obrigatórios — ver `react-hook-form.mdc`.
- **`useEffect` e estado inicial:** ver `react-effects-and-data.mdc` — dados no servidor, fetch só em handlers, sem `setState` em efeitos.

## UI

- Shadcn + tokens semânticos (`bg-background`, `text-foreground`, `border-border`) — ver `ux.mdc`.
- Controlos de formulário: sempre `@/components/ui` — ver `shadcn-components.mdc` (sem `<select>` / checkbox / radio nativos).
- Altura relativa ao viewport: preferir `dvh`, nunca `vh` — ver `ux.mdc`.
- Formulários clínicos multi-secção (anamnese, roteiros, GMFM): `ClinicalWorkspaceShell` + módulo com `render(ctx)` — ver `clinical-workspace.mdc` (caminho real 1→2→3; sem lib wizard/EHR).
- Não hardcodar cores (`bg-white`, `text-gray-900`).

## Exclusão (obrigatório)

- **Nunca** usar `window.confirm()` para apagar dados.
- Usar sempre `DeleteConfirmDialog` de `@/components/ui/delete-confirm-dialog`.
- Envolver o botão de excluir como `children`; personalizar `title` / `description`; `onConfirm` chama a action.

```tsx
<DeleteConfirmDialog
  title="Excluir lançamento?"
  description="Esta ação não pode ser desfeita."
  onConfirm={handleDelete}
  disabled={pending}
>
  <Button variant="destructive">Excluir</Button>
</DeleteConfirmDialog>
```

## Estrutura (quando criar feature nova)

```
src/features/[feature]/
  actions.ts
  components/   # UI client reutilizada entre rotas do domínio
  hooks/        # só client
  _lib/         # helpers puros
```

- **UI partilhada entre rotas:** ver `route-shared-ui.mdc` — nunca importar `_components` de outra rota.
