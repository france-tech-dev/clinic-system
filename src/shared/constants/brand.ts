/** Logo oficial do produto Movi (mascote). */
export const BRAND_LOGO = "/marketing/mascot.png" as const;

/** Nome de produto (fallback de branding / PDF). */
export const DEFAULT_APP_NAME = "Movi Clínicas";

/** Logo padrão em PDF e branding quando a clínica não enviou a própria. */
export const DEFAULT_PRINT_LOGO = BRAND_LOGO;
